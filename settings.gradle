pluginManagement {
    repositories {
        maven {
            url 'https://search.maven.org/'
        }
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id 'org.kordamp.gradle.jandex' version "${jandexPluginVersion}"
    }
}

rootProject.name = 'notch-shell-utils'